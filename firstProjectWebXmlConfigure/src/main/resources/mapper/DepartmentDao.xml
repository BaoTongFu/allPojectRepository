<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.btf.online.dao.DepartmentDao">
	<select id="queryDepartment" resultType="com.btf.online.entity.Department">
		SELECT
		department_id,parent_department_id,department_name,department_address,department_level,create_time,create_user_id,update_time,update_user_id
		FROM DEPARTMENT
		ORDER BY update_time
		DESC
	</select>
</mapper>